__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
venv/
.vscode/
.idea/
.DS_Store
Thumbs.db

# Per-machine Claude Code permission state — not something other contributors
# need. Tracked historically; untracked from 2026-05-16 onward.
.claude/settings.local.json

# Local-only working artifacts (regenerable from src/ingest.py)
data/raw/
data/processed/

# Legacy PDF output location — kept ignored in case anyone still has an old
# reports/ folder from before PDFs moved to docs/reports/. The current build
# (src/report.py) writes to docs/reports/<slug>.pdf, which IS committed —
# the leading slash here keeps this rule scoped to the project-root reports/
# only, not to docs/reports/.
/reports/
